%p
  = link_to @course, class: 'btn btn-sm btn-link' do
    %i.icon.ion-android-arrow-back
    Back
= render partial: 'courses/course_header', locals: {course: @course}
.col-md-10.offset-md-1
  .card.course-header_card
    .card-body
      %p.course-category{style: "color: #{@course.category_color}"}
        %i.icon.ion-edit
        = @course.category_title
      %h2.course-title= @course.title

      = render partial: 'course_indicator', locals: {course: @course, current: @lesson}

      %h2.lesson-title= @lesson.title
      .card-text.lesson-text= @lesson.body.html_safe
      - if CompletionHandler.complete?(current_user, @lesson)
        = link_to 'Completed', lesson_incomplete_path(@lesson), method: :delete, class: 'btn btn-sm btn-white-text', style: "background-color: #{@course.category_color}"
      - else
        = link_to 'Mark Complete', lesson_complete_path(@lesson), method: :post, class: 'btn btn-sm btn-light'

- if current_user&.admin?
  = link_to 'Edit lesson', edit_lesson_path(@lesson), class: 'btn btn-sm btn-link'
  %button{"data-path" => "#{link_to 'Delete course', lesson_path(@lesson), method: :delete, class: 'btn btn-danger'}", "data-target" => "#deleteModal", "data-toggle" => "modal", :type => "button", id: 'deleteModal', class: 'btn btn-sm btn-link'} Delete lesson

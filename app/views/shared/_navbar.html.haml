%header
  %nav.navbar.navbar-default
    .container
      .navbar-header
        %button.navbar-toggle.collapsed{"aria-controls" => "navbar", "aria-expanded" => "false", "data-target" => "#navbar", "data-toggle" => "collapse", :type => "button"}
          %span.sr-only Toggle navigation
          %span.icon-bar
          %span.icon-bar
          %span.icon-bar
        %a.navbar-brand{:href => root_path}
          %p West Coast Weight Loss Center
      #navbar
        %ul.nav.navbar-nav.navbar-right
          - if current_user&.admin?
            %li= link_to 'Panel', panel_path
          - if user_signed_in?
            %li= link_to 'Lessons', lessons_path
            %li= link_to 'Billing', billing_path
            %li= link_to 'Settings', edit_user_registration_path
            %li= button_to 'Sign out', destroy_user_session_path, method: :delete
          - else
            %li= link_to "Home", root_path
            %li= link_to "Choose Us", choose_us_path
            %li= link_to "Our Program", our_program_path
            %li= link_to "Get Started", get_started_path
            %li= link_to "Contact Us", contact_us_path
            -# %li= link_to "Sign in", new_user_session_path, class: "#{'nav-active' if params[:action] == 'new' && params[:controller] == 'users/sessions'}"
            -# %li= link_to "Sign up", new_user_registration_path, class: "#{'nav-active' if params[:action] == 'new' && params[:controller] == 'devise/registrations'}"
